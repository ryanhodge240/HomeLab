apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

helmCharts:
  - name: traefik
    repo: https://traefik.github.io/charts
    version: 38.0.0   # use your desired version
    releaseName: traefik
    namespace: traefik
    valuesFile: values.yaml

resources:
  - certificate.yaml