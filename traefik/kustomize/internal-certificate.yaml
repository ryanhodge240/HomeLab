apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: lan-wildcard-cert
  namespace: traefik
spec:
  secretName: lan-wildcard-tls
  dnsNames:
    - argo.lan
    - portfolio.lan
    - ryanhodge.lan
    - ai.ryanhodge.lan
    - pihole.lan
    - lrw.lan
    - ha.lan
    - geo-app.lan
    - "*.lan"
    - lan
  issuerRef:
    name: internal-ca-issuer
    kind: ClusterIssuer